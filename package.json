{
  "name": "kmip-pkcs11-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Slide-deck generation for the kmip_pkcs11 project. The Word documents are generated by the Python generate_*.py scripts; only the deck needs Node.",
  "scripts": {
    "deck": "node generate_overview_deck.js",
    "pdf": "soffice --headless --convert-to pdf --outdir . KMIP_PKCS11_Overview_Deck.pptx",
    "deck:all": "npm run deck && npm run pdf"
  },
  "devDependencies": {
    "pptxgenjs": "^3.12.0"
  }
}
